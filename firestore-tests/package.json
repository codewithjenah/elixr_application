{
  "name": "elixr-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firebase Emulator security-rules tests for firestore.rules (daily quest board/claim + leaderboard XP). Not part of the Flutter app build.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-elixr --only firestore \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
