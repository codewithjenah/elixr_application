fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
numpy==2.4.6
opencv-python==4.13.0.92
ultralytics==8.4.80
lap==0.5.13
# Both distributions expose the same ``onnxruntime`` import; keep them
# mutually exclusive so Windows gets DirectML and other platforms keep CPU ORT.
onnxruntime-directml==1.23.0; sys_platform == "win32"
onnxruntime==1.23.2; sys_platform != "win32"
mediapipe==0.10.9
matplotlib==3.11.1
# Windows DirectShow camera identity (FriendlyName + DevicePath). Optional at
# runtime on non-Windows; discovery falls back to OpenCV indices without it.
comtypes==1.4.10
