rules_version = '2';
// Diagnostic-only open rules for the ELX-011 Firestore emulator audit.
// Do not use these rules in production and do not deploy them.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
