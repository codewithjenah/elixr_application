---
description: Firebase Authentication, Firestore mappings, rules, indexes, and persisted data compatibility
globs: ["lib/data/**/*.dart", "lib/services/auth_service.dart", "firestore.rules", "firestore.indexes.json", "firebase.json", ".firebaserc", "lib/firebase_options.dart"]
alwaysApply: false
---
# Firebase and persisted data

- Firebase Authentication UID is the user identity and user document ID.
- Keep Firebase operations behind the existing repository/Firestore adapter boundaries for new code.
- Preserve snake_case fields used by existing documents: examples include `full_name`, `user_id`, `movement_name`, `duration_seconds`, `feedback_type`, and `created_at`.
- Treat server timestamps as nullable or pending when reading immediately after writes.
- A model change must consider Dart models, Firestore mapping, repository queries, security rules, indexes, existing documents, and README/API documentation.
- A new compound query must be reconciled with `firestore.indexes.json`.
- UI visibility is not authorization. Keep ownership checks in `firestore.rules`.
- Never store passwords, auth tokens, service-account keys, raw frames, or unnecessary sensitive data in Firestore.
- Do not deploy Firestore rules/indexes without explicit user intent and human review.
- Do not point the app at a different Firebase project casually; generated options and project files must remain consistent.
- Preserve session/feedback linkage and user ownership when changing write flows.

## Leaderboard (Firestore)

- `leaderboard/{userId}` uses the Firebase UID as document ID and `user_id`.
- `leaderboard_processed_sessions/{sessionId}` is the duplicate-award idempotency marker.
- Leaderboard awards must be idempotent; do not award the same session twice.
- The source `sessions/{sessionId}` document must exist and belong to the authenticated user.
- The processed marker must match the session, user, score, and `xp_awarded` (25) according to `firestore.rules`.
- Keep lifetime aggregate fields synchronized with `LeaderboardEntry`, `LeaderboardAwardPlan`, `LeaderboardRepository`, and `firestore.rules`: `display_name`, `total_xp`, `sessions_completed`, `score_sum`, `average_score`, `best_score`, `last_session_at`, `updated_at`, `last_awarded_session_id`.
- Daily/monthly leaderboard fields are optional for legacy documents but all-or-none within each period group: `daily_key`/`daily_xp`/`daily_sessions_completed`/`daily_score_sum`/`daily_average_score`/`daily_best_score` and the matching `monthly_*` fields. Session awards derive period keys from the source session's immutable server-stamped `created_at`; quest awards derive them from the verified Manila-day board. Older events preserve newer period aggregates.
- Keep leaderboard query ordering synchronized with `firestore.indexes.json`: all time uses `total_xp` then `best_score`; Today uses current `daily_key`, `daily_xp`, then `daily_best_score`; This month uses current `monthly_key`, `monthly_xp`, then `monthly_best_score`; every ordering ends with document ID ascending for deterministic ties.
- Preserve authenticated leaderboard reads and own-document write constraints; do not weaken marker update/delete restrictions.
- Display-name-only updates must not alter XP or score aggregates.
- The current client transaction model is suitable for the controlled capstone environment but must not be described as server-authoritative or hostile-client-proof.
- Camera settings are stored locally and must not be described as Firestore data.

## Public profiles (Firestore)

- `public_profiles/{userId}` uses the Firebase UID as document ID and `user_id`.
- Root document fields (see `publicProfileRootKeys` in `firestore.rules`): `user_id`, `display_name`, optional `profile_picture_url` (HTTPS), `visibility` (`public` | `private`), `created_at`, `updated_at`, `schema_version`.
- Root documents are authenticated read for any signed-in user (basic directory / leaderboard identity regardless of lock state).
- New accounts seed via `seedNewAccountPublicProfile` with `visibility: public` when the root is missing; idempotent — never overwrites an existing root.
- Repair/backfill paths (`ensurePublicProfile`, `syncClaimedAchievementProjections`, `updatePublicIdentity`) create missing roots as `visibility: private` (fail-closed; no silent widening of legacy accounts).
- Parser and rules default missing or unknown `visibility` to `private`.
- Protected subdocs (`details/summary`, `sessions/{sessionId}`, `achievements/{achievementId}`) are readable only when `canReadProtectedPublicProfile(userId)` — owner OR `visibility == public`.

For data changes, verify mapping round trips, null timestamps, empty data, permission behavior, and query/index alignment.
