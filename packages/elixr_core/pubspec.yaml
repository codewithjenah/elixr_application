name: elixr_core
description: Shared ELIXR user model and Firebase auth repository.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  characters: ^1.4.1
  cloud_firestore: ^6.6.0
  firebase_auth: ^6.5.4
  firebase_core: ^4.11.0
  firebase_storage: ^13.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
